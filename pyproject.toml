[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "banking-data-governance-control-plane"
version = "1.0.0"
description = "Metadata-driven banking governance, privacy, lineage and access-control assessment"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

